<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <application
        android:name=".android.OhMyBiasApp"
        android:label="@string/app_name"
        android:icon="@mipmap/ic_launcher"
        android:allowBackup="true"
        android:theme="@style/AppTheme">

        <activity
            android:name=".MainActivity"
            android:launchMode="singleTop"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
            <!-- 檔案管理員／瀏覽器點 .cskin 直接開本 app（MainActivity 詢問後套用）。
                 pathPattern 以路徑尾端比對副檔名；多組 pattern 是 PatternMatcher
                 對檔名含多個「.」的既知繞法。host="*" 是 pathPattern 生效的前提。 -->
            <intent-filter>
                <action android:name="android.intent.action.VIEW" />
                <category android:name="android.intent.category.DEFAULT" />
                <category android:name="android.intent.category.BROWSABLE" />
                <data android:scheme="file" />
                <data android:scheme="content" />
                <data android:host="*" />
                <data android:mimeType="*/*" />
                <data android:pathPattern=".*\\.cskin" />
                <data android:pathPattern=".*\\..*\\.cskin" />
                <data android:pathPattern=".*\\..*\\..*\\.cskin" />
                <data android:pathPattern=".*\\..*\\..*\\..*\\.cskin" />
            </intent-filter>
        </activity>

        <!-- 常用語設定（全螢幕編輯器）— 設定頁與鍵盤 ♥ 面板「設定」鍵都開這個 -->
        <activity
            android:name=".UserPhrasesActivity"
            android:label="常用語設定"
            android:windowSoftInputMode="adjustResize"
            android:exported="false" />

        <!-- 自訂工具列（設定頁「主題」下） -->
        <activity
            android:name=".ToolbarSettingsActivity"
            android:label="自訂工具列"
            android:exported="false" />

        <service
            android:name=".keyboard.OhMyBiasImeService"
            android:label="@string/ime_name"
            android:permission="android.permission.BIND_INPUT_METHOD"
            android:exported="true">
            <intent-filter>
                <action android:name="android.view.InputMethod" />
            </intent-filter>
            <meta-data
                android:name="android.view.im"
                android:resource="@xml/method" />
        </service>
    </application>
</manifest>
