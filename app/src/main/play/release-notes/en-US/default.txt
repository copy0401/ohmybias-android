0.4.1 — delete selection, diagonal space swipes
• Fix: Backspace did nothing on selected text (select-all deleted nothing; a partial selection deleted the character before it). It now removes the selection, like the system keyboard
• Space key diagonal gestures: up-right for Zhuyin lookup, up-left for homophone mode. Settings guide corrected (horizontal drag moves the cursor)
• Paired punctuation commits in one step; candidate bar is pre-built so the first suggestion no longer stutters
